<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main bg-zinc-900 min-h-screen w-full">
        <div class="form p-10 text-white">
            <form action="/create" class="" method="post">
                <input type="text" placeholder="Title" class="block px-5 py-2 bg-zinc-800 w-full rounded-md outline-none" name="title">
                <textarea placeholder="Description" class="resize-none block px-5 py-2 bg-zinc-800 mt-2 w-full rounded-md outline-none" name="details"></textarea>
                <input type="submit" value="Create Task" class="mt-2 bg-blue-600 px-5 py-2 rounded-md">
            </form>
        </div>
        <div class="tasks p-10 flex gap-3 flex-wrap">
            <% if(files.length > 0){ %>
                <% files.forEach((val)=>{ %>
                    <div class="task w-72 px-3 py-4 bg-zinc-800 rounded-md" >
                        <h1 class="text-white text-3xl tracking-tighter"><%= val %></h1>

                        <div class="w-full flex justify-between items-center mt-3">
                            <a href="/file/<%= val %>" class="text-blue-500 inline-block">Read More.</a>
                            <a href="/edit/<%= val %>" class="text-zinc-600 ">Rename File</a>
                        </div>
                       
                    </div>
            <% }) %>

            <% } else{ %>
                <h3 class="text-zinc-600 ">No task yet.</h3>
          <%  } %>   
            
        </div>
    </div>
</body>
</html>